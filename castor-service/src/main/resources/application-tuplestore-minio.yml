spring:
  config:
    activate:
      on-profile: k8s
carbynestack:
  castor:
    minio:
      endpoint: ${MINIO_ENDPOINT}
      bucket: minio-castor-bucket
      access-key: dummy-key
      secret-key: dummy-secret
